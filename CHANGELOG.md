# 更新日志

## [1.0.5] - 2026-05-31

### 修复

- 修复光标位于多行函数签名第一行行首时，条件2未跨行扫描导致范围线不触发的问题；现在最多向下扫描 60 行查找 `) {`，覆盖超长参数列表
- 修复条件2在纯空行上也会触发的问题；光标所在行为空行时不再尝试触发
- `#define` 等预处理器宏行整体排除，其中的括号不再影响括号匹配计数

---

## [1.0.4] - 2026-05-31

### 重构

- 彻底重写触发逻辑，改为两条独立判断：条件1（当前行末尾为 `) {`）和条件2（光标在行首，向右查找 `) {`），更简洁可靠
- 新增 `sanitizeLine` 有状态处理，跨行跟踪 `/* */` 块注释状态，注释内的括号不再参与匹配计数
- 删除旧的 `enclosingOpen`、`sigStart`、`nextOpen` 等函数，代码结构大幅简化

---

## [1.0.3] - 2026-05-31

### 修复

- 修复返回类型单独成行（如 `bool`、`std::string` 等）时未被纳入范围线的问题
- 修复范围线与代码首字符存在空格间距的问题；范围线现在自动对齐到代码块的最小缩进处
- 改名为"代码范围线"，准确反映插件支持函数、类、结构体等所有代码块的实际范围

## [1.0.2] - 2026-05-30

### 修复

- 修复 `class` / `struct` 及中文类定义行（如 `模板<类型名 T> 类 Foo {`）无括号时不触发范围线的问题
- 添加中文模板前缀 `模板<...>` 的识别，使其与 `template<...>` 同样被纳入签名起始行
- 修复 `nextOpen` 未过滤字符串/注释中 `{` 导致的偶发错位

## [1.0.1] - 2026-05-30

### 修复

- 修复光标在变量声明语句（如 `std::lock_guard<std::mutex> x(arg);`）上时，错误地触发函数范围线高亮并跨越到下一个函数的问题
- 以分号 `;` 结尾的行现在被正确识别为语句而非函数签名，不再触发范围线

## [1.0.0] - 2026-05-01

### 初始发布

- 在光标所在函数/块左侧显示完整垂直范围线，覆盖函数签名到结尾 `}`
- 支持多行参数声明（签名每一行均纳入范围）
- 支持 `template<>`、`inline`、`static`、`virtual`、`constexpr` 等前缀行
- 通过 `sanitizeLine` 过滤字符串字面量、正则字面量和注释中的括号，避免误判
- 一键选中整个函数快捷键 `Ctrl+Alt+C`
- 自定义范围线颜色（`functionScopeLine.color`）和宽度（`functionScopeLine.lineWidth`）
- 支持所有使用 `{}` 大括号的语言：C、C++、Java、JavaScript、TypeScript、C#、Go、Rust 等
